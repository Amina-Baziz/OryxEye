# ── Python Dependencies (CNN/Flask API) ───────────────────────
tensorflow==2.21.0
flask==3.1.2
flask-cors==6.0.2
pillow==12.1.1
numpy==2.4.2
scikit-learn==1.8.0

# ── Node.js Dependencies (server/) ────────────────────────────
# Automatically installed via: cd server && npm install
# express, mongoose, cors, dotenv, multer, axios, natural, form-data

# ── React Dependencies (frontend/) ────────────────────────────
# Automatically installed via: cd frontend && npm install
# react, react-dom, react-scripts